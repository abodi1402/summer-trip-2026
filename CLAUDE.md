# ذاكرة وسياق مشروع مخطط رحلة روسيا 2026

نظام تفاعلي متكامل لتنسيق رحلة الأصدقاء إلى روسيا صيف 2026، مع مزامنة لحظية بين جميع الأجهزة.

---

## 👥 المسافرون (Travelers)

| # | الاسم | الجوال | الدور | اسم الجواز |
|---|------|--------|------|------------|
| 1 | عبدالله الزهراني | `0506230054` | منظم الرحلة (Admin) | Abdullah Mohammed I Alzahrani |
| 2 | عبدالعزيز الحميد | `0555255181` | المشرف المالي | Abdulaziz Mohammed A Alhumaid |
| 3 | حسن الدوسري | `0599967664` | مسؤول الخدمات اللوجستية | Hassan Marzouq M Almujib |
| 4 | فهد بن جديد | `0590099919` | منسق الأنشطة والبرامج | Fahad Dakhel F Bin Jadid |

> ⚠️ اسم حسن في الجواز "**Almujib**" وليس "**Aldosari**" - الدوسري لقب قبلي فقط. كل إجراءات السفر تستخدم اسم الجواز.

---

## 🌐 الوجهة والمسار

**روسيا** (موسكو + سان بطرسبرغ) | **24 يونيو → 5 يوليو 2026** | **12 يوماً**

البنية: **4 + 4 + 4**
- **أيام 1-4:** موسكو (ضمنها يوم الريف)
- **أيام 5-8:** سانت بطرسبرغ (يوم 5 = قطار سابسان)
- **أيام 9-12:** موسكو مرة ثانية (يوم 12 = العودة)

---

## 🚀 الروابط والنشر

- **التطبيق المباشر:** https://trip-26.vercel.app
- **مستودع GitHub:** https://github.com/abodi1402/summer-trip-2026
- **منصة النشر:** Vercel (auto-deploy عند `git push`)
- **قاعدة البيانات:** Supabase (`nvvuulsctlxezdxcswvb.supabase.co`)

---

## 🛠 التقنيات (Stack)

| الطبقة | التقنية |
|--------|---------|
| الواجهة | **React 19** + Vite + Tailwind CSS v4 |
| الأيقونات | Lucide React |
| الخط | **Tajawal** (Google Fonts) |
| قاعدة البيانات | **Supabase** (PostgreSQL + Realtime) |
| الاستضافة | **Vercel** (CDN + auto deploy) |
| اللغة | عربي (RTL) كامل |

---

## 🎨 الهوية البصرية (Russian-inspired)

> الهوية مُلهمة من علم روسيا لكن بألوان أهدأ للاستخدام اليومي:

| العنصر | اللون | الاستخدام |
|--------|------|-----------|
| Primary (Navy) | `#2A3F7E` | الأزرار، التأكيد، النص الرئيسي |
| Primary Dark | `#1B2D64` | hover states، التدرجات |
| Accent Red | `#D52B1E` | الأخطاء، التنبيهات، عناوين الصفحات، الإجراءات الحرجة |
| Russian Flag (Official) | `#0036A7` + `#D62718` + `#FFFFFF` | فقط في شرائط العلم الزخرفية في hero |
| Cream Background | `#FAF7F2` | خلفية التطبيق |
| Card Border | `#ECE6DC` | حدود البطاقات |
| Dark Text | `#14172A` | النصوص الأساسية |

**قواعد التصميم:**
- بطاقات بيضاء بظل خفيف (لا glassmorphism، لا نيون)
- **بدون إيموجي** في الواجهة (تم إزالة 50+ موقع)
- شارات تصنيف ملونة
- ترحيب عامي ودي شخصي لكل مسافر (3 صياغات عشوائية)
- شريط العلم الرسمي يظهر فقط كأكسسوار في hero

---

## 🗄 جداول قاعدة البيانات (Supabase)

```
itinerary               — مسار الرحلة 12 يوم (مع places JSONB متعدد الأماكن)
bookings                — الحجوزات (مع attachments JSONB للـ PDFs)
tasks                   — المهام المشتركة (مع is_critical)
expenses                — المصاريف (مع category)
traveler_passwords      — كلمات السر (sync بين الأجهزة)
fund_contributions      — قطة كل عضو (phone + paid_sar + target_sar)
trip_settings           — إعدادات عامة JSONB (reserve_fund، إلخ)
```

كل الجداول فيها **RLS مفعّل** مع سياسة "allow_all" (لأن المشروع لأصدقاء فقط).

ملف SQL الكامل: `SUPABASE_GUIDE.md`

---

## 📂 هيكل الملفات

```
.
├── src/
│   ├── App.jsx              ← التطبيق الكامل (4600+ سطر)
│   ├── index.css            ← Tajawal font + Russian palette + tile styles
│   ├── supabaseClient.js    ← Supabase client مع anon key
│   ├── main.jsx             ← نقطة الانطلاق
│   └── assets/
│       └── russia_hero.png  ← صورة الـ hero (قبة سانت باسيل)
├── public/
│   └── favicon.svg
├── SUPABASE_GUIDE.md        ← SQL الكامل + migrations
├── CLAUDE.md                ← هذا الملف
└── package.json
```

### بنية App.jsx (التبويبات)

| التبويب | الوصف |
|---------|------|
| `dashboard` | الرئيسية - hero + 6 tiles + جاهزية الفريق |
| `itinerary` | جدول الأيام - 12 يوم + فلتر مدن + أماكن متعددة لكل يوم |
| `bookings` | الحجوزات - تبويبات تصنيف + بطاقات مضغوطة + PDFs |
| `tasks` | المهام المشتركة - شارات حرجة + assign |
| `expenses` | المالية - 3 صناديق + 9 تصنيفات + جدول قطة |
| `personal` | حقيبتي وأوراقي - مستلزمات + وثائق (خاصة) |
| `proposals` | المقترحات والتصويت |
| `superadmin` | لوحة عبدالله فقط (تصفير كلمات السر، إعدادات) |

---

## ⚙️ أوامر التشغيل

```bash
# تطوير محلي (port 5173)
npm run dev

# بناء للإنتاج
npm run build

# تثبيت حزم
npm install <package-name>

# دفع التحديثات (Vercel ينشر تلقائياً)
git add -A && git commit -m "..." && git push
```

> 🍎 الجهاز يستخدم Node من PATH الافتراضي. لا حاجة لأي export يدوي.

---

## 🔐 الأمان

- ✅ كل كلمات السر محفوظة في Supabase (مزامنة بين الأجهزة)
- ⚠️ كلمات السر بنص عادي (ليس bcrypt) - مقبول لأن المشروع خاص بـ4 أصدقاء
- ✅ تسجيل دخول أول مرة يطلب تغيير `123456`
- ✅ المنظم (عبدالله) يقدر يصفّر كلمة سر أي عضو من لوحة التحكم
- ✅ Browser autofill مفعّل (`autocomplete="tel"` + `"current-password"`)
- ✅ RLS مفعّل على كل الجداول

---

## 🎯 الميزات الرئيسية

### الواجهة
- 📱 Mobile-first design (مُحسّن للجوال أولاً)
- 🔄 Realtime sync بين الأجهزة (Supabase Channels)
- 🚀 PWA-friendly (يفتح كتطبيق)
- 🌙 RTL كامل
- ⬇️ FAB (Floating Action Button) في كل صفحة فيها فورم
- 🔝 شريط ناڤيقيشن سفلي بسيط (4 عناصر فقط)
- ↩️ زر "الرئيسية" يظهر في كل تبويب فرعي

### وظائف
- ✈️ **تذاكر الطيران:** مرفوعة بـ PDF (Qatar Airways - 4 مسافرين - SAR 11,700)
- 💰 **القطة المشتركة:** يتم تعديلها من المنظم، تحفظ تلقائياً بـ debounce 500ms
- 📊 **تتبع المصروفات:** 9 تصنيفات ملونة + شارات
- 🤝 **خطة التسوية الذكية:** تحسب أبسط تحويلات لتسوية الحسابات الشخصية
- 📎 **رفع مرفقات PDF/صور** للحجوزات (Blob URL للفتح، Web Share API للمشاركة)
- 🗺 **أماكن متعددة لكل يوم** في الجدول (مطعم + مقهى + معلم + جولة)
- 👑 **قائد اليوم** مع checklist مهام تشغيلية
- 🔔 **بث تنبيهات عاجلة** من المنظم
- 🗳 **تصويت ومقترحات** جماعي

---

## 🧭 العد التنازلي والترحيب

- **العد التنازلي:** بالثواني (يوم/ساعة/دقيقة/ثانية)
- **التاريخ المستهدف:** `2026-06-24` الساعة 08:00
- **الترحيب:** عبارات عامية مختلفة لكل عضو، تُختار مرة واحدة عند الدخول (لا ترتجف)

---

## 📋 ملاحظات للجلسات القادمة

1. **لا تستخدم إيموجي** في الواجهة - المستخدم لا يحبها (تُضعف الجودة)
2. **استخدم ألوان روسيا الرسمية** فقط في شرائط العلم الزخرفية
3. **خط Tajawal** هو القرار النهائي (تم مقارنته مع Cairo + Readex)
4. **العناوين h2** بالأحمر `#D52B1E`
5. **زر الإضافة العائم (FAB)** يتعرّف على الفورم تلقائياً ويُمرّر إليه
6. **شخصية المنظم (عبدالله):** متواضع، يستخدم "صمّمه وأعدّه" بدلاً من "بإشراف"
7. **رابط الموقع القصير:** `trip-26.vercel.app`
8. عند تعديل القاعدة، **حدّث SUPABASE_GUIDE.md** بالـ SQL الجديد
9. **GitHub Token:** المستخدم استخدم `ghp_NQ360...` (لازم يُلغى بعد كل جلسة)

---

## 🚧 ميزات مستقبلية محتملة

- 🔄 Realtime sync لجدول `fund_contributions` و `trip_settings`
- 💾 Supabase Storage (بدل JSONB) للملفات الكبيرة
- 🔐 Supabase Auth الرسمية بدلاً من نظام الكلمات الحالي
- 📱 PWA manifest + offline support
- 🌍 خريطة تفاعلية للأماكن
- 📊 إحصائيات تفصيلية للميزانية بالشهر/الأسبوع

---

**آخر تحديث للوثيقة:** 27 مايو 2026 | **آخر commit:** `eb7eac3`
